# =============================================================================
# Glass MCP Server Configuration
# =============================================================================
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# SECURITY: Never commit the .env file to version control!

# =============================================================================
# Required Settings
# =============================================================================

# ServiceDesk Plus base URL
# - Include the scheme (https://)
# - Do NOT include trailing slash or /api/v3 suffix
# - Examples:
#   - On-premises: https://servicedesk.example.com
#   - Cloud: https://sdpondemand.manageengine.com
SDP_BASE_URL=https://servicedesk.example.com

# ServiceDesk Plus technician API key
# - Get this from: SDP Admin > Developers > API > Technician API Key
# - This key determines what operations Glass can perform
# - Treat this like a password - never share or commit it
# - Use a dedicated technician account with minimal required permissions
SDP_API_KEY=your_api_key_here

# =============================================================================
# Optional Settings
# =============================================================================

# Log level for Glass
# - Levels: error, warn, info (default), debug, trace
# - Use 'debug' to see API requests/responses (API key is always redacted)
# - Use 'trace' for very verbose output (not recommended in production)
# RUST_LOG=glass=info

# You can also set levels per-module:
# RUST_LOG=glass=debug,glass::sdp_client=trace

# =============================================================================
# Claude Code Configuration
# =============================================================================
#
# Add Glass to ~/.claude/claude_code_config.json:
#
# {
#   "mcpServers": {
#     "glass": {
#       "command": "/path/to/glass",
#       "env": {
#         "SDP_BASE_URL": "https://servicedesk.example.com",
#         "SDP_API_KEY": "your-api-key-here"
#       }
#     }
#   }
# }
#
# Or if you prefer using the .env file:
#
# {
#   "mcpServers": {
#     "glass": {
#       "command": "/path/to/glass",
#       "cwd": "/path/to/glass-directory"
#     }
#   }
# }
